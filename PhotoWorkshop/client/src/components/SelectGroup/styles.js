export const styles = theme => ({
    formControl: {
        margin: theme.spacing.unit,
        width: '134px',
        minWidth: 120
    },
    selectEmpty: {
        marginTop: theme.spacing.unit * 2
    }
});
