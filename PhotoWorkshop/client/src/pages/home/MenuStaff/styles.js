export const styles = theme => ({
    root: {
        // display: 'flex'
        // marginLeft: '40%'
    },
    paper: {
        marginRight: theme.spacing.unit * 2
    },
    appBarButton: {
        backgroundColor: '#9FA8DA'
    }
});
