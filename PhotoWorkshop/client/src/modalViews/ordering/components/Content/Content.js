import React from 'react';

import { Form } from '../Form';
import { ButtonList } from '../ButtonList';

const Content = () => (<div>
    <Form/>
    <ButtonList/>
</div>);

Content.propTypes = {
};

export default Content;
